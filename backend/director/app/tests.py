from django.test import TestCase

# Create your tests here.

# TODO: convert from tests in shell
# song = Song.objects.create(name="hello", song_id="1")
# host = User.objects.create(name='host')
# party = Party.objects.createParty(host=host)
# guest = party.join('guest')
# ret = party.requestSong(guest, song)
# party.getQueue()